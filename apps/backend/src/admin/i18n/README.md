# Admin i18n

You can define custom i18n translations in this directory.

The translations should be exported as a default object from `index.ts`. The keys of the object should be the locale codes, and the values should be the translations.
